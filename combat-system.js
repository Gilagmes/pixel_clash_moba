// Combat 3.0 is injected by server-launcher.js so the existing server remains the source of truth.
// This module documents the status model used by the runtime patch.
module.exports={version:'3.0.0',statuses:['stun','slow','stealth','shield','execute']};
